//#region Imports

import { makeStyles } from '@material-ui/core/styles';

//#endregion

const useStyles = makeStyles(() => ({
    icon: {
        color: '#FFFFFF'
    },
    text: {
        color: '#FFFFFF'
    }
}));

export default useStyles;
