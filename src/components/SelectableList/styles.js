//#region Imports

import { makeStyles } from '@material-ui/core/styles';

//#endregion

const useStyles = makeStyles(() => ({
    content: {
        width: '100%',
        marginBottom: 20
    }
}));

export default useStyles;
